DATABASE_URL=

# --- Pipeline behaviour (optional, sensible defaults exist in code) ---
# How many days of history to pull on the very first run (backfill).
BACKFILL_DAYS=365

# Seconds to wait between tickers so we don't hammer Yahoo Finance.
REQUEST_DELAY_SECONDS=1.0
